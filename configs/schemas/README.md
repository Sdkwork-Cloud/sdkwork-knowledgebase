# Config Schemas

Purpose: config schema definitions and validation rules.

Owner: SDKWork Knowledgebase maintainers.

Allowed content: JSON Schema, YAML schema, and config validation definitions.

Forbidden content: live credentials, secrets, runtime user config, and local overrides.

Related specs: `../../sdkwork-specs/CONFIG_SPEC.md`, `../../sdkwork-specs/ENVIRONMENT_SPEC.md`.
