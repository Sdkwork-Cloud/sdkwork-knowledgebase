# Config Examples

Purpose: example config files for reference and onboarding.

Owner: SDKWork Knowledgebase maintainers.

Allowed content: annotated example configs and setup guides.

Forbidden content: live credentials, secrets, local overrides, and runtime user config.

Related specs: `../../sdkwork-specs/CONFIG_SPEC.md`, `../../sdkwork-specs/DOCUMENTATION_SPEC.md`.
