# Test Fixtures

Purpose: safe test fixtures for cross-package and contract tests.

Owner: SDKWork Knowledgebase maintainers.

Allowed content: fixture data, mock responses, test configuration, and sample files.

Forbidden content: real secrets, tokens, private customer data, runtime state.

Related specs: `../sdkwork-specs/TEST_SPEC.md`.
