export interface ConsumeGroupKnowledgebaseLaunchTicketRequest {
  /** Opaque one-time IM launch ticket. It must never be persisted, logged, or copied into a URL query parameter. */
  ticket: string;
}
